import styled from "styled-components";

const Columns = styled.div`
  text-align: center;
  color: #fff;
  display: flex;
  flex-direction: row;
  width: 100%;
  height: 100%;
  gap: 2rem;
`;

export default Columns;
